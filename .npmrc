# Relax npm's strict peer resolver. The Angular 21 toolchain and PrimeNG 21 pull
# a web of peer ranges that npm 10 can otherwise refuse to reconcile inside a
# workspaces monorepo (e.g. deprecated webpack build peers, @angular/cdk).
# Applies locally and on Vercel (which runs npm install).
legacy-peer-deps=true
