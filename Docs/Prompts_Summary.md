# Ensyncro — prompts summary

A running log of prompts/decisions from this chat and from Claude Code, in execution order. Claude Code should append its own entries here after each executed + pushed change (see instructions at the bottom).

## Chat (planning, PRD, deck)

| # | Prompt / decision | Outcome |
|---|---|---|
| 1 | Initial brief: build Ensyncro, a funding marketplace (demo/staging/prod), Angular + PostgreSQL, app included, high security, non-coder workflow | Scoped tool split: Claude Code for build, this chat for planning/docs/deck |
| 2 | "do you think claude or kimi would fit better" | Compared honestly; stuck with Claude for the integrated chat + Claude Code workflow |
| 3 | PRD outline + wireframes | PRD v1 drafted; homepage wireframe + palette options shown |
| 4 | Connect to GitHub for auto commit/push/deploy | No GitHub MCP connector available; Vercel connector is read-only. Recommended Claude Code (real git credentials) + Vercel auto-deploy-on-push instead |
| 5 | Move to Claude Desktop / Claude Code | Handoff plan given: PRD.md into repo, first scaffold prompt provided |
| 6 | Keep documents in doc, excel, ppt | Generated Ensyncro_PRD_v1.docx, Ensyncro_Execution_Plan.xlsx, Ensyncro_Pitch_Deck.pptx (+ kept .md as fast-editing source) |
| 7 | Stats question: demo vs staging vs prod data | Demo = seeded, Staging = real live data, Production = threshold-gated (25+ founders, 10+ investors) before showing real counts |
| 8 | Messaging in v1? | Recommended against full messaging for v1; "Request intro" action instead. Full messaging deferred to phase 2 |
| 9 | Palette decision | Locked: Charcoal green (#2C2C2A / #1F6D3B / #F6F5F1 / #FFFFFF / #E1DFD6 / #7A7870) |
| 10 | Maintain a running Prompts_Summary doc across chat + Claude Code | This file created |

## Claude Code (build, commits, pushes)

| # | Date | Prompt given to Claude Code | Commit / PR | Status |
|---|---|---|---|---|
| 1 | 2026-07-19 | Scaffold monorepo skeleton: Angular + NestJS + Prisma, env config for demo/staging/production, git init, README | `6dc5415` | Pushed |
| 2 | 2026-07-19 | Maintain this Prompts_Summary log; append a row per commit within the same commit | `753ba30` | Pushed |
| 3 | 2026-07-19 | Document the "same commit, fill hashes at push" logging convention | `1f6a6a0` | Pushed |
| 4 | 2026-07-19 | Init git repo, connect GitHub remote (origin), commit and push | [`origin/main`](https://github.com/sagarradia/ensyncro/tree/main) | Pushed |
| 5 | 2026-07-19 | Scaffold Angular frontend app: root shell, home page, routing, favicon, .editorconfig; verified `ng build` | `0388a49` | Pushed |
| 6 | 2026-07-19 | Scaffold NestJS backend app: HealthModule (/api/health + DB status), tsconfig.build, validation deps, placeholder Prisma model; verified build + live health request | `2ff6f74` | Pushed |
| 7 | 2026-07-19 | Plan task #4 — Env config for demo/staging/production: boot-time env validation (fail-fast + prod warnings), monorepo-root .env loading, ENVIRONMENTS.md; add execution plan to repo | `a31c4d4` | Pushed |
| 8 | 2026-07-19 | Plan task #5 — Auth: signup, login, JWT, mock OTP. Minimal User/OtpCode/RefreshToken models (replaces ScaffoldProbe), AuthModule (bcrypt, JWT access + revocable refresh, mock OTP), guard + /me; verified build, routes, validation/guard, security primitives (DB round-trip pending a database) | `8da08a8` | Pushed |
| 9 | 2026-07-19 | Fix Vercel build TS2305 (@prisma/client missing Role/OtpChannel): confirmed enums in schema; add prisma generate via postinstall + prebuild in apps/api; verified from a clean state with npm run api:build | `c978a3c` | Pushed |
| 10 | 2026-07-19 | Confirm frontend is its own app (apps/web) and make it independently deployable: add apps/web/vercel.json (static SPA, outputDirectory dist/web/browser, SPA rewrite, asset caching) + README deploy notes | `d8612a0` | Pushed |
| 11 | 2026-07-19 | Fix blank deployed frontend (NG0908): add zone.js polyfill in angular.json; wire build-time env injection (scripts/set-env.mjs via prebuild, APP_ENV/API_BASE_URL) replacing fileReplacements, add Environment type; verified render + no console errors in browser + injected URL in bundle | `6eb30d2` | Pushed |
| 12 | 2026-07-19 | Option A environment separation: add DEPLOYMENT.md runbook (branch↔Vercel-env↔Neon mapping, env matrix with placeholders/no secrets, setup + verification steps), link from ENVIRONMENTS.md, and create long-lived demo + staging branches | `21ca2c2` | Pushed |
| 13 | 2026-07-19 | Update DEPLOYMENT.md to reflect real Neon setup: production maps to Neon `main` branch (kept, not renamed) in the Vercel-managed project neon-bronze-pocket; staging/demo branches added | `660acd2` | Pushed |
| 14 | 2026-07-20 | Simplify to a single environment: delete demo+staging git branches (local+origin); rewrite DEPLOYMENT.md + ENVIRONMENTS.md for one branch/one Vercel project per app/one Neon DB; update PRD §8 + §10 (multi-env deferred); remove orphaned demo-scoped Vercel env var (no custom envs existed). Neon branch/project deletions handed to user (permanent-deletion policy); production-branch dashboard check flagged | `5526fba` | Pushed |
| 15 | 2026-07-20 | (1) Investigate duplicate Vercel API projects — `ensyncro-api` is the real one (repo-linked, full Neon integration env vars, prod URL, Prisma-fix history); `ensyncro-api-new` is redundant (zero env vars/no DB) — user deletes in dashboard. (2) Strip multi-env scaffolding: remove build:demo/staging(/production) scripts, production-only set-env.mjs; verified `npm run build`. (3) Production Branch not exposed by CLI (`vercel project inspect` shows framework/root but no git branch; REST API 403) — user must check dashboard | `2398627` | Pushed |
| 16 | 2026-07-20 | (1) Delete redundant ensyncro-api-new — permanent-deletion policy, gave `vercel project rm` command instead. (2) Set ensyncro-api Framework→Other + Build Command `npm run build` via `vercel project update`; verified prisma generate runs via prebuild (clean build); add NestJS serverless entrypoint for Vercel (api/index.ts + src/serverless.ts + configure-app.ts + apps/api/vercel.json), verified /api/health boots via the serverless app. (3) No CLI flag for Git Production Branch (project update/inspect help + REST 403) — dashboard only | `899d4ba` | Pushed |
| 17 | 2026-07-20 | Add APP_ENV=production to ensyncro-api Production via CLI (was absent — no `rm` needed; that's why /api/health showed demo). Found the serverless deploy was failing with "No Output Directory named public" (Framework=Other expects static output) — fix apps/api/vercel.json (framework null, buildCommand, outputDirectory=public) + add public/.gitkeep; push to trigger a production redeploy and confirm env: production | `7c3bc7c` | Pushed |
| 18 | 2026-07-20 | Wire frontend→API: add API_BASE_URL=https://ensyncro-api.vercel.app/api to ensyncro-web Production (was missing); also set CORS_ORIGIN=https://ensyncro-web.vercel.app on ensyncro-api (was defaulting to localhost:4200, would have blocked browser calls). Redeployed both to production; verified the deployed bundle bakes the API URL and a cross-origin fetch from ensyncro-web.vercel.app → /api/health returns 200 (env: production, db: up). CLI/dashboard work — no repo change | `8984820` | Pushed |
| 19 | 2026-07-20 | Day 2 kickoff: (1) generate + set JWT_ACCESS_SECRET/JWT_REFRESH_SECRET on ensyncro-api Production (openssl piped straight to `vercel env add` — values never printed). (2) Task #10 DB migration: create initial Prisma migration (offline `migrate diff`; users/otp_codes/refresh_tokens + enums) and `migrate deploy` to the prod Neon DB (used neonctl unpooled connection string since Vercel Sensitive vars aren't readable); verified tables exist via a count query. (3) Redeploy ensyncro-api; ran full auth e2e against prod — signup → verify both OTPs → ACTIVE → login returns a valid JWT (role FOUNDER) + refresh token. Commit the migration files | `3ccca37` | Pushed |
| 20 | 2026-07-20 | Pre-RBAC cleanup: (1) SELECT-confirmed the test user row in prod (the only row; 2 otp_codes + 1 refresh_token cascade) and gave the user the exact DELETE to run (didn't execute — permanent-deletion policy). (2) Added DIRECT_URL (unpooled) as a Vercel env var on ensyncro-api Production (neonctl string via --value, never printed). (3) Documented the Vercel Sensitive-vars-are-write-only gotcha + neonctl connection-string workaround in DEPLOYMENT.md (new Database migrations section) | `45a864e` | Pushed |
| 21 | 2026-07-21 | Day 2 unit 1 — task #10 (rest of schema): add PRD §7 models (founder_profiles, investor_profiles, admin_profiles, admin_invites, data_room_files, data_room_access_log, saved_shortlist, intro_requests, cms_content, platform_stats) + AdminRole/FundingStage/InvestorType/DataRoomVisibility/ShortlistStatus/IntroRequestStatus enums. Deliberately NO `messages` table — replaced by intro_requests per PRD §10 "Request intro" decision. Migration generated by diffing live DB and applied to prod; verified 14 tables / 9 enums | `5a8fa89` | Pushed |
| 22 | 2026-07-21 | Day 2 unit 2 — task #6 (RBAC): @Roles decorator + RolesGuard (top-level FOUNDER/INVESTOR/ADMIN from the JWT) and @AdminRoles + AdminRolesGuard for admin sub-roles (SUPER/FINANCE/LEGAL/OPS), reading AdminProfile from the DB so sub-role changes take effect immediately rather than at token expiry; SUPER satisfies any sub-role. AdminModule with guarded /api/admin/me + /api/admin/stats. Verified: 10/10 guard unit tests (allow+deny paths) and a live 401 on the guarded route | `3c4b308` | Pushed |
| 23 | 2026-07-21 | Day 2 unit 3 — task #9 (admin bootstrap + invites): `npm run admin:bootstrap` script creating the first SUPER admin directly in the DB (password supplied by the operator via prompt/env, never by Claude; refuses if a SUPER already exists), plus invite-only creation of further admins — POST/GET /api/admin/invites (SUPER only, token returned once and stored only as a SHA-256 hash, 72h expiry) and public POST /api/admin/invites/accept to set a password and activate. Verified: script guardrails (missing email, weak password), all 5 admin routes mapped, 401 on guarded invite create, 400 DTO validation on the public accept | `db6f7e3` | Pushed |
| 24 | 2026-07-21 | Verified the invite flow + RBAC sub-roles END-TO-END against production (seeded an OPS invite, accepted over HTTP, invite single-use enforced on replay, logged in, then live RBAC: /admin/me 200, /admin/stats 200 as OPS, POST /admin/invites 403 "Requires admin role: SUPER"). Then task #7 backend: founder profile API — GET/PUT /api/founder/profile guarded @Roles(FOUNDER), userId taken from the token (never the body) so a founder can only touch their own row; upsert so wizard steps save incrementally. Verified: null-before, create, update-same-row, validation 400, cross-role 403, anon 401 | `fc64f37` | Pushed |
| 25 | 2026-07-21 | Frontend auth shell (prerequisite for tasks #7/#8): login + two-step signup (details -> email/mobile OTP -> auto-login), TokenStorageService (localStorage, trade-off documented), AuthService with signals, functional authInterceptor that only attaches the JWT to our own API origin, and authGuard/roleGuard route guards (UX only — API still enforces). Also fixed a duplicate rxjs install (root 7.8.2 vs nested apps/web 7.8.1) that broke the build with cross-copy Observable type errors — `npm dedupe`. Verified in a real browser end-to-end: signup -> OTP (mock codes pre-filled) -> auto-login -> nav shows logged-in state -> JWT + refresh token + ACTIVE user persisted | `e66f820` | Pushed |
| 26 | 2026-07-21 | Task #7 — founder onboarding wizard at /founder/onboarding: 3 steps (company / funding / about), guarded by roleGuard('FOUNDER'), loads any existing profile and saves progress on each step via PUT (upsert) with the final step setting completed. Added ProfileService + FounderProfile/FundingStage models and wizard styles. Verified in a real browser: logged-out deep link redirected to /login?returnUrl=..., login returned to the wizard, all 3 steps walked through, and the founder_profiles row in production matched every field with completed=true | `c959674` | Pushed |
| 27 | 2026-07-21 | Task #8 — investor onboarding wizard at /investor/onboarding (+ backing API): GET/PUT /api/investor/profile guarded @Roles(INVESTOR) with ArrayMinSize(1) on investorTypes; 3-step wizard (identity / focus / about) with the 11-value PRD §3 InvestorType multi-select and comma-separated sectors. Verified in a real browser: investor signup → OTP → auto-login routed to /investor/onboarding, all 11 type checkboxes rendered, advancing with none selected was blocked ("Pick at least one investor type"), and the investor_profiles row in production persisted investorTypes [ANGEL, SEED_VC, FAMILY_OFFICE] + sectors array + ticket range with completed=true | `e045c03` | Pushed |
| 28 | 2026-07-21 | Task #18 — one-click demo logins ("Pitch shortcuts"): seed script for 3 fixed demo accounts (SUPER admin, founder "Verdant Labs" + completed profile, investor "Lumen Capital" + completed profile w/ 3 types & 3 sectors), a dedicated public POST /auth/demo-login that only issues tokens for those allow-listed emails (also checks role + ACTIVE) and is gated by DEMO_LOGINS_ENABLED, plus a login-screen shortcuts section (form -> divider -> 3 role buttons). No demo credentials in frontend code. Verified in the browser for all 3 roles: Founder -> /founder/onboarding ("Company profile saved"), Investor -> /investor/onboarding ("Lumen Capital is set up"), Admin -> / with its JWT returning 200 + adminRole SUPER from /api/admin/me | `4077af9` | Pushed |
| 29 | 2026-07-21 | Harden demo access: downgrade the demo admin from SUPER to OPS (demo-accounts.ts + re-seeded the production row; button label now "Ops-level platform access") and set DEMO_LOGINS_ENABLED=false on ensyncro-api Production so shortcuts stay off until the pitch. Re-verified in the browser: admin shortcut still logs in, /admin/me returns adminRole OPS, /admin/stats 200 (OPS allowed), and POST /admin/invites now 403 "Requires admin role: SUPER" — i.e. it genuinely lost SUPER powers. Real admin@ensyncro.com untouched at SUPER | `babf6f6` | Pushed |
| 30 | 2026-07-21 | Gate the pitch shortcuts on a real flag instead of letting them error: add public GET /api/config (appEnv + demoLoginsEnabled, non-sensitive only) and an AppConfigService that fails CLOSED (flags read disabled until/unless the response says otherwise), then hide the entire Pitch shortcuts block — divider, intro and buttons — when disabled. Verified both states in the browser: enabled -> divider + 3 buttons render and the Investor shortcut still logs in to /investor/onboarding; disabled -> /api/config returns false, zero shortcut elements render, and the password form is untouched | `d0e6885` | Pushed |
| 31 | 2026-07-21 | Task #11 backend — discovery endpoints: GET /api/founders/discover (sector / stage / funding range) and GET /api/investors/discover (investor type / sector / ticket-range overlap), both JWT+Roles gated so investors browse founders and founders browse investors (admins see both); only completed profiles are discoverable; paginated with total. Verified live: founder->investors 200 / founder->founders 403, investor->founders 200 / investor->investors 403, admin 200 on both, anon 401, every filter returns the expected counts, and a bad enum gives 400 | `d0c077c` | Pushed |
| 32 | 2026-07-21 | Tasks #12/#13 backend — data room + audit log: upload (founder-only, 10MB cap), owner listing, visibility control, POST /files/:id/access -> 60s signed link, GET /files/:id/content?token=... which re-authorises at read time and audits the view, plus GET /access-log for founders. Byte storage behind a FileStorage interface (Postgres impl for now; PRD §8 S3 swap is one implementation) + data_room_file_blobs migration. Verified live incl. adversarial: no token 403, login-JWT reused as signed token 403, garbage token 403, guessed file id 403, investor on PRIVATE 404 (indistinguishable from missing), signed-token replay onto another file 403; owner + admin + (after VISIBLE_TO_INVESTORS) investor read exact bytes; audit log recorded both real views and 403s for non-founders | `ccf071c` | Pushed |
| 33 | 2026-07-21 | Tasks #11-13 frontend: Discover pages per role (filter controls + card results; nav link routes by role so investors get /discover/founders and founders /discover/investors), founder data room UI (upload w/ visibility, list, per-file visibility change, "View securely" -> signed link) and the audit-log table. Client-side 10MB pre-check plus 413 handling. Verified in the browser: uploaded a REAL file through the file input, 11MB file rejected with a clear message and input cleared, visibility toggled to VISIBLE_TO_INVESTORS, signed fetch returned exact bytes w/ Cache-Control no-store, investor got 201 on the shared file (bytes match) but 404 on a PRIVATE one and 403 listing the founder's data room, and the audit table rendered the investor's view | `cd003c0` | Pushed |
| 34 | 2026-07-21 | S3 storage migration: implement S3FileStorage against the existing FileStorage interface (AWS SDK v3, creds from AWS_* env), backend chosen at runtime so local/dev falls back to Postgres when AWS isn't configured. /content now AUDITS then 302-redirects to a 60s presigned URL instead of proxying bytes — required so a serverless function never streams large media (200MB video in the next unit) while keeping read-time re-authorisation + audit intact. Per-file document cap 10MB -> 25MB (API + web). Local regression on the Postgres fallback passed; real-S3 verification runs against production since Vercel Sensitive vars can't be read locally | `292409f` | Pushed |

---

## Logging convention

A commit's hash is derived from its contents, so a commit can never contain its own final hash. To keep rows in the **same commit** as the change (as requested) while still producing references that resolve on GitHub:

1. When a prompt produces a commit, append its row in **that same commit**, with the `Commit / PR` cell set to `` `_pending_` ``.
2. **Right before pushing**, replace every `` `_pending_` `` cell with the real, now-final short hash (in one reconciliation step), then push. Rows for earlier commits reference ancestors, so their hashes resolve correctly on the remote.

The `Date` uses the current date; the `Prompt` cell is a one-line summary of what was asked; `Status` tracks committed/pushed state.

> Original instruction (kept for reference): _Maintain a file at `docs/Prompts_Summary.md`. Every time you execute a prompt that results in a commit, append a row to the "Claude Code" table: the date, a one-line summary of what was asked, the commit hash or PR link, and status. Do this as part of the same commit, not a separate one._
| 35 | 2026-07-21 | Fix S3 uploads 500ing in production: AWS_REGION held the AWS console's display label ("Asia Pacific (Sydney) ap-southeast-2") rather than the region code, and the SDK uses region as a hostname component. Corrected the Vercel env var, and normalised AWS_REGION in config so a pasted console label still resolves; a region that can't be parsed while S3 credentials ARE present now logs an explicit error naming the variable instead of silently falling back to Postgres for production uploads | `5f13391` | Pushed |
| 36 | 2026-07-21 | Verify the S3 unit against production with real credentials + document it. Full adversarial suite re-run and green: signed URL now returns 302 -> presigned S3 (not an API stream), bytes match exactly for owner and for investor after VISIBLE_TO_INVESTORS, missing/garbage/login-JWT tokens 403, cross-file replay 403, investor 404 on PRIVATE, audit rows written for every view, investor 403 on the access log. Separately confirmed the bucket itself is private: unsigned object GET 403 AccessDenied, unsigned bucket listing 403, tampered signature 403, presigned link carries X-Amz-Expires=60. DEPLOYMENT.md documents the AWS_* variables, the storage-backend fallback and the AWS_REGION label gotcha | `05b6cbb` | Pushed |
| 37 | 2026-07-22 | Direct-to-S3 uploads, per-kind size caps and the 500MB founder quota. Probing production showed uploads over ~4.5MB fail at the platform edge with FUNCTION_PAYLOAD_TOO_LARGE before reaching any handler, so the 25MB document cap was unenforceable and a 200MB pitch video could never be posted through a function at all. Added POST /files/upload-url (validates, reserves a PENDING row, returns a presigned PUT with Content-Length and Content-Type signed) and POST /files/:id/complete (HeadObject reads the real stored size, so a small declared size cannot smuggle a large object past the cap or quota; oversize uploads are deleted and rejected). Added MediaKind (DOCUMENT/PITCH_VIDEO/PRODUCT_IMAGE/LOGO) with per-kind caps 25/200/10MB and content-type prefix enforcement, UploadStatus so unconfirmed rows are invisible to every read path, GET /usage, DELETE /files/:id, and the 500MB per-founder quota with video exempt. Migration applied to production | `f7c12a5` | Pushed |
| 38 | 2026-07-22 | Founder media, public product page and gated financials/funding history (items 2-5). Pitch video is either a validated YouTube/Vimeo/Loom link or an uploaded PITCH_VIDEO file, never both; only provider+id are stored and the embed URL is rebuilt server-side so a founder-supplied URL is never reflected into an iframe (rejects javascript:/data: schemes, lookalike hosts like youtube.com.evil.com, and ids outside [A-Za-z0-9_-]). Product page adds productName/tagline/productDescription/categories/logo, ungated for any signed-in user. Financials (MRR/ARR/burn/runway/use of funds) + milestones and round-by-round funding history are gated with the same visibility rule as the data room, return an identical 404 whether missing or not shared, and record every non-owner view in a new profile_section_access_log surfaced to the founder | `ca45d83` | Pushed |
| 39 | 2026-07-22 | Request intro (item 7 / PRD §10's replacement for v1 messaging). POST /intro-requests, GET (sent + received in one call) and PATCH to accept or decline. Founders and investors can only reach each other, and a request aimed at an admin or a disabled account returns the same 404 as a bad id; self-requests and duplicate pending requests are refused; only the recipient can respond and a decision is final. Contact details (email/mobile) are attached only after acceptance, so the accept step is what actually discloses anything | `970520e` | Pushed |
| 40 | 2026-07-22 | Frontend for items 2-7. New UploadService performs the two-step direct-to-storage upload with a real progress bar, deliberately sending no Authorization header to the storage host (the interceptor only attaches the JWT to our own origin, and forwarding it to a third party would leak the token); it holds progress at 99% until the API confirms the bytes, and falls back to multipart when the backend cannot presign. New /founder/content page: pitch video by link or upload, product name/tagline/description/category tags/logo, financials + milestones, funding rounds, per-section visibility controls, a quota bar, and the log of who opened the gated sections. New /founders/:userId product page renders the video (iframe src passed through DomSanitizer only because the API rebuilds it from provider+id, never raw input), shows a locked state for sections this viewer cannot see rather than firing a request that 404s, and carries the Request intro action. New /intros inbox for accept/decline with contact details appearing only after acceptance | `f0415ff` | Pushed |
| 41 | 2026-07-22 | Browser verification of items 2-7 against production, plus a card form-styling fix. Verified in the browser: founder content page renders quota, embedded Vimeo pitch video, milestones, funding rounds and the gated-section access log; investor sees the product page with financials unlocked but funding history showing the locked state, proving the two gates are independent in the UI as well as the API; intro request sent from the product page, accepted by the founder, and contact details appeared only after acceptance. One blocker found and NOT fixable from here: the S3 bucket has no CORS configuration, so a browser PUT to the presigned URL fails with "Failed to fetch" while the same request from Node succeeds (CORS is browser-only). Uploads through the UI stay blocked until the bucket CORS policy is added in the AWS console; the API, presigning and completion all work | `64b40fc` | Pushed |
| 42 | 2026-07-22 | Frozen frontend stack (partner decision): begin migrating the hand-styled Angular app to PrimeNG + Tailwind + a custom design system. Unit 1 — tooling + design tokens: installed primeng@18 + @primeng/themes@18 + primeicons + tailwindcss@3.4 + tailwindcss-primeui; created an Ensyncro PrimeNG preset (definePreset on Aura) that maps the locked charcoal-green palette to theme tokens (primary=#1F6D3B scale, surface scale bone→sand→stone→charcoal, content/text/formField tokens) as the single source of truth — no hex scattered in components. Wired providePrimeNG + provideAnimationsAsync, forced single light theme, and set a CSS layer order (tailwind-base, legacy, primeng, tailwind-utilities) so PrimeNG components win on migrated screens while the untouched pages keep their legacy CSS. Tailwind preflight disabled to avoid regressing unmigrated pages. Migrated the app shell (header/footer) to Tailwind + tokens with a PrimeNG button. Verified locally: charcoal header, green accent button, palette tokens resolve, legacy pages intact | `156ede8` | Pushed |
| 43 | 2026-07-22 | PrimeNG migration unit 2 — auth screens. Login and signup rebuilt on PrimeNG components (p-card, pInputText, p-select for the role, p-button with loading state, p-message, p-divider) with Tailwind layout and theme tokens; no logic change (same reactive forms, validators, submit/verify flows, demo-shortcut buttons). Verified locally: themed cards/inputs/buttons, green accent, select panel highlight using the primary.50 token, step indicator active state in accent green | `cc32872` | Pushed |
| 44 | 2026-07-26 | Per user request, upgraded the frozen frontend stack to the latest. Went to Angular 22 + PrimeNG 22 first, but PrimeNG 22 introduced a license gate (@primeui/license-manager) that stamps an "Invalid PrimeUI License" watermark until a paid/registered key is set — surfaced this; user chose the latest license-free line. Landed on Angular 21.2.18 + PrimeNG 21.1.9 + @primeuix/themes 2.0.3 + @angular/cdk 21 (themes package renamed from @primeng/themes to @primeuix/themes; preset import updated). Monorepo TypeScript: Angular 21 needs TS 5.9 and the NestJS API already builds on 5.9, so both workspaces share a single hoisted TS ~5.9 (root pin). Added root .npmrc legacy-peer-deps=true so npm reconciles the Angular/PrimeNG peer web locally and on Vercel. Verified: web build clean (905kB/195kB gz), API build clean, dev-server runtime shows the themed screens with no license watermark and a clean console | `9d68c68` | Pushed |
| 45 | 2026-07-26 | PrimeNG migration unit 3 — onboarding wizards. Founder and investor wizards rebuilt on PrimeNG (p-card, pInputText, p-select for stage, pTextarea, p-checkbox for the investor-type multi-select, p-button, p-message) with Tailwind layout + tokens; step logic, validators and save-per-step behaviour unchanged. Fixed a readonly-array assignability error by passing a mutable copy of FUNDING_STAGES to p-select options. Web build clean (AOT); functional verification deferred to the deployed-site pass since the guarded routes need a real session | `42090c5` | Pushed |
| 46 | 2026-07-26 | PrimeNG migration unit 4 — discover pages. Discover-founders and discover-investors rebuilt on PrimeNG (p-card filter panel + result cards, pInputText, p-select with showClear, p-tag for stage/investor-type pills, p-button, p-message) with a responsive Tailwind grid; filter/search/reset logic unchanged, founder card still links to the product page. Web build clean | `7614a61` | Pushed |
| 47 | 2026-07-26 | PrimeNG migration unit 5 — data room. Rebuilt on PrimeNG (p-card upload panel + file cards, p-select for visibility, p-tag for the visibility badge, p-table for the audit log, p-button with pi icons) and Tailwind grid. Upload keeps the exact custom flow via a hidden native file input triggered by a p-button (so onFileSelected is untouched); per-file visibility select switched from a native change event to p-select onChange, and changeVisibility now takes the value directly. Raised the initial-bundle warning budget to 1.5MB to account for p-table. Web build clean, no budget warnings | `8916ad5` | Pushed |
| 48 | 2026-07-26 | PrimeNG migration unit 6 — browser verification on the deployed Angular 21 + PrimeNG 21 build, plus a minor discover-founders fix (made the founder website link block-level so it no longer runs into the "View product page" link). Verified end-to-end against production with real data: demo login (all roles) + themed login/shortcuts; founder wizard loads the profile and binds it into pInputText/p-select; investor wizard step 2 shows the p-checkbox multi-select pre-checked in accent green from real data; discover-investors and discover-founders render filter panels + result cards with p-tag pills; data room shows the upload panel, file cards with visibility p-selects/p-tags, and the p-table audit log — all with no license watermark and a clean console. Migration complete | `7b4a210` | Pushed |
| 49 | 2026-07-26 | Follow-up functional re-test of the interactive flows the PrimeNG migration actually rewired (requirement #4), on the deployed site: login form submit — empty submit renders the reactive-form validation errors, proving p-button type=submit still fires ngSubmit; discover filter — sector=Fintech + Apply narrows 3 founders to the one Fintech match; data room visibility change — selecting a new value in the per-file p-select fires the reworked changeVisibility(value) handler, the API persists it, the notice and the p-tag update (reverted after). Confirms no functional regression beyond the earlier render/data-load checks. (Also confirmed several earlier "failures" were harness artifacts — computer-clicks not landing on p-button/overlay elements — not code issues.) No code change | `n/a (verification)` | Logged |
| 50 | 2026-07-26 | Brand logo. Replaced the text "Ensyncro" wordmark in the nav with the white-on-transparent logo (nav is charcoal), and placed the dark-on-transparent logo on the light-background brand surfaces: centred above the login and signup cards, in the home hero, and in the footer next to the copyright. Sized by height with w-auto to preserve the stacked lockup's aspect. Assets served from src/assets/logo. Verified rendering at desktop width | `726797a` | Pushed |
| 51 | 2026-07-26 | Responsive nav. Below the md breakpoint the header collapses to logo + hamburger (pi-bars/pi-times toggle); a stacked mobile menu panel drops below the header with all role-aware links + a full-width Log out / Sign up, closing on navigation and on logout. Desktop row unchanged at md+ (email hidden until lg to avoid crowding). Verified at 375px: hamburger opens/closes the panel, links navigate and dismiss it, and the signup p-select dropdown opens and is usable at mobile width | `55782ad` | Pushed |
| 52 | 2026-07-26 | Mobile audit — data room audit table. Wrapped the p-table in an overflow-x-auto container with a min width so the wide 4-column log (long email + timestamp) scrolls within its own box instead of forcing the whole page to scroll sideways on small screens; formatted the raw ISO timestamp via the date pipe. Other migrated screens already stack single-column on mobile (responsive Tailwind grids from the PrimeNG migration); full deployed-site verification at 375/768 follows | `9aa55e3` | Pushed |
| 53 | 2026-07-26 | Fix: commit the logo image files. The earlier logo commit staged only the templates that reference the logos, not the PNGs themselves (they were untracked), so they 404'd on the deployed site and rendered as broken-image alt text in the nav and footer. Added both assets to the repo | `c1035db` | Pushed |
| 54 | 2026-07-26 | Mobile audit fix — aligned the Apply/Reset and wizard Back/Save button rows (added items-center to the flex containers on both discover pages and both onboarding wizards); they were vertically offset by ~24px. Verified on the deployed site at 375px: authenticated hamburger menu lists the role links + full-width Log out and closes on navigation, data room audit table scrolls within its box with no page-level horizontal overflow, discover filter + cards stack single-column, and the nav logo loads | `3a110b0` | Pushed |
| 55 | 2026-07-26 | Deepen the founder (Investee) profile to the full VMB structured model — unit 1, schema + migration (foundational). Added 8 child tables (promoters, group_companies, products_services, competitors, swot_items, risk_items, future_plans, benchmark_peers) with cascade delete, plus FounderProfile scalar fields: usp / businessModel / marketSize / targetSegment / marketGeography (public), and annualRevenue / grossMarginPct / cashBalance / priorYearArr (extra financial inputs for computed ratios), and risksVisibility / futurePlansVisibility gating columns. New enums SwotCategory + RiskSeverity; ProfileSection extended with RISKS + FUTURE_PLANS. Company Journey reuses the existing FounderMilestone table (no duplication). Migration is fully additive; applied to production and client regenerated; API builds clean | `78e6a72` | Pushed |
| 56 | 2026-07-26 | Deep founder profile — units 2+3, the API. Added CRUD for all eight structured sections (promoters, group companies, products/services, competitors, SWOT, risks, future plans, benchmark peers), each scoped to the caller's own profile on create and delete. Public scalar detail (usp/businessModel/market fields) folded into the existing product update; product page now returns the public sections + Company Journey (the milestones, moved out of gated financials). Gated Risks and Future Plans reuse the same gate()/mayView()/audit path as Financials via the extended ProfileSection map, with per-section visibility and viewer-side GETs on /founders/:id/risks and /future-plans. Financials extended with extra inputs (annualRevenue/grossMargin/cashBalance/priorYearArr) and computed ratios (ARR growth, runway-from-cash, revenue/employee, burn multiple, ARR:MRR) plus benchmark peers. GET /founder/profile/sections returns everything for the editor. API builds clean | `95c7abf` | Pushed |
| 57 | 2026-07-26 | Deep founder profile — units 4+5, the UI. Extended the founder-content editor with every new section as autosaving/resumable panels (each add/remove reloads from the server): USP/business model/market, promoters, group companies, products & services, competition, SWOT (categorised grid), company journey (the milestones, now its own public section), plus extended financials (annual revenue, gross margin, cash, prior-year ARR) showing the computed ratios and a benchmark-peers table, and the two gated sections Risks and Future Plans each with their own PRIVATE/SHARED/VISIBLE visibility control. Rebuilt the public product page to render all public sections + journey, the ratios/benchmarking inside gated financials, and locked-state cards for gated Risks/Future Plans (fetched only when access says so, like financials). Web builds clean | `1ad2adb` | Pushed |
| 58 | 2026-07-26 | Deep founder profile — unit 6, browser verification on the deployed site. API tested exhaustively first (every section persisted; ratios correct — ARR growth 68%, burn multiple 5.59x, runway 14mo, rev/employee 27,273; public sections on product page; gating independent per section with the investor view audited). Then through the UI: founder editor rendered all new sections; added a promoter via the form and it persisted ("Asha Rao 55%"); added a risk + a future plan and shared ONLY Risks with investors; the investor's product page then showed the promoter (public) and the risk (shared) while Future Plans stayed locked (independent gating), with financials ratios rendering. Feature complete | `0ad0090` | Pushed |
| 59 | 2026-07-26 | PRD housekeeping: adopt Docs/Ensyncro_PRD_v2.md (VMB-aligned, the single source of truth) into git; delete the superseded Docs/Ensyncro_PRD_v1.md (was the only old PRD — no literal docs/PRD.md existed); repoint the README's PRD link v1→v2 | `c70bcda` | Pushed |
| 60 | 2026-07-26 | PRD v2 is now maintained incrementally in place by Claude Code (edited section-by-section per relayed planning instructions, not replaced wholesale from a download). Added a maintenance note near the top of Docs/Ensyncro_PRD_v2.md pointing to this decision log | `7150b7f` | Pushed |
| 61 | 2026-07-26 | Deleted the stray untracked Docs/Ensyncro_PRD_v1 - Copy.md. Pull showed the repo already up to date — the §15.2 update mentioned wasn't present (no local or remote change), so recorded it in place per the incremental-PRD workflow: §15.2 confirms structured-info sections are autosaving profile-editor panels, not onboarding wizard steps, overriding the earlier wizard-steps wording | `a8e606d` | Pushed |
| 62 | 2026-07-26 | Broader Investee scope (PRD v2 §7/§8) — unit 1, schema + migration. Added tables shareholders (gated cap-table basics), key_customers, suppliers (public), projected_financials (gated forward-looking), and sector_masters (seeded with 10 sectors — admin-extensible, replaces the hardcoded sector). FounderProfile scalar fields: classification (natureOfBusiness[] / businessStage / companyClassification), funding requirement (fundingRequirementType / instrument / use summary — public), manufacturing + operations (public), and shareholding/projectedFinancials visibility columns. New enums NatureOfBusiness, BusinessStage, CompanyClassification, FundingRequirementType; ProfileSection extended with SHAREHOLDING + PROJECTED_FINANCIALS. Fully additive; applied to production, sectors seeded, client regenerated, API builds clean | `d63aff1` | Pushed |
| 63 | 2026-07-26 | Broader Investee scope — unit 2, the API (also fixes the build the schema-only commit left broken: the ProfileSection→visibility map and gate() select had to become exhaustive over the two new gated sections). Added PUT /founder/profile/meta (classification: natureOfBusiness[]/businessStage/companyClassification/sector; funding requirement: type/instrument/use-summary/amount; manufacturing+operations), GET sectors (masters), CRUD for shareholders/customers/suppliers/projected-financials, gated owner+viewer GETs for shareholding and projected-financials (same gate/mayView/audit path), and a computed profile completion score (weighted presence across ~28 checks) surfaced in ownSections. Product page returns the new public sections + shareholding/projectedFinancials access flags. API builds clean | `df7c507` | Pushed |
| 64 | 2026-07-26 | Broader Investee scope — unit 3, the UI. Editor gains autosaving panels for: Classification (nature-of-business multi-select, business stage, company classification, sector from the masters dropdown), Funding requirement (type/amount/instrument/use-summary), Operations (manufacturing/operations), Key customers, Key suppliers, and two gated panels — Shareholding and Projected financials — each with their own visibility control; plus a Profile-completeness bar driven by the server-computed score. Product page renders the new public sections (classification chips, funding requirement, operations, customers, suppliers) and adds locked-state cards for gated Shareholding + Projected financials, fetched only when the access flags allow. Web builds clean | `415b630` | Pushed |
| 65 | 2026-07-26 | Broader Investee scope — unit 4, browser verification on the deployed site. API tested first (sectors master, classification/funding meta with completion score, all lists persist, shareholding+projected gated independently, product page public meta + access flags). Then through the UI: saved classification (nature multi-select/stage/class/sector-from-masters); added a shareholder and a projected-financial via their panels and both persisted; profile completeness rose 64%→71% as sections filled; on the investor's product page the classification chips + funding requirement + operations show publicly, Shareholding (shared) is visible while Projected financials (private) stays locked — independent per-section gating confirmed. Feature complete | `7ff03e0` | Pushed |
| 66 | 2026-07-26 | Added §17 "Site map (current, per role)" to PRD v2 — a reality snapshot audited against app.routes.ts + the API controllers, not the target IA. Table of the 11 real routes (URL/page/what-it-does/access), a per-role summary, and explicit reality notes: Admin has NO dedicated UI (only reachable via the ADMIN-guarded discover/product pages + a handful of /api/admin/* endpoints and a bootstrap script; sub-roles SUPER/FINANCE/LEGAL/OPS enforced server-side but unmanaged in UI; platform config not built), and Consultant has zero pages/routes/API | `8750b19` | Pushed |
| 67 | 2026-07-26 | Scaffold the 4th role (Consultant, PRD v2 §4) — unit 1, schema. Added CONSULTANT to the Role enum, a ConsultantType enum (CA/CS/ADVOCATE/VALUER), a ConsultantProfile model (name/firm/type/registrationNumber/completed) and a ConsultantInvite model mirroring AdminInvite (hashed token, expiry, createdBy). Additive migration applied to production; client regenerated; API builds clean (no exhaustive Role switch broke) | `79f2dfb` | Pushed |
| 68 | 2026-07-26 | Consultant scaffold — unit 2, the API. Self-contained ConsultantModule: ConsultantInvitesService (mirrors admin invites — hashed token, 72h TTL, returned once); POST /consultant/invites/accept (public, token=credential → creates a CONSULTANT user + ConsultantProfile, auto-issues a token pair); admin/consultant-invites create+list gated to Super Admin (JwtAuthGuard+RolesGuard ADMIN + AdminRolesGuard SUPER); and GET /consultant/me + PUT /consultant/profile gated to CONSULTANT. Wired into app.module; API builds clean | `cce18f4` | Pushed |
| 69 | 2026-07-26 | Consultant scaffold — unit 3, the frontend. Added CONSULTANT to the Role type + a ConsultantType/labels/profile model; homeRouteFor routes consultants to /consultant. New public /consultant/accept page (set a password from the invite link → auto-logs-in) and a gated /consultant dashboard (roleGuard CONSULTANT) — placeholder "your assigned engagements will appear here" plus a basic profile editor (name/firm/registration; discipline shown read-only). Nav shows a Dashboard link for consultants (desktop + mobile). Web builds clean | `dcd5878` | Pushed |
| 70 | 2026-07-26 | Consultant scaffold — unit 4, browser verification + PRD sync. Verified end-to-end on the deployed site: admin consultant-invite creation is SUPER-gated (OPS admin 403, SUPER 201); accepting the invite (set password) auto-logged-in and landed on /consultant as CONSULTANT ("Chartered Accountant", engagements placeholder); the role guard redirected a consultant away from /data-room to /consultant; and logging in via the normal login form landed the consultant on /consultant. Updated PRD §17 site map to reflect the new reality (consultant is now scaffolded, not zero) — added the two consultant routes, the admin consultant-invite endpoints, and rewrote the Consultant per-role note. A demo consultant (consultant.demo@ensyncro.com) now exists | `6a3f61c` | Pushed |
| 71 | 2026-07-26 | Deal Management (PRD v2 §5, the central object) — unit 1, schema. Deal model linked 1:1 to intro_request (a deal is created only when an intro is accepted, never from scratch), with founder + investor parties, a DealStage enum (Interest→Meeting→NDA→Data Room→Due Diligence→Offer→Negotiation→Closed Won/Lost) and a DealStatus (OPEN/WON/LOST). DealEvent table is the append-only timeline (kinds: CREATED/STAGE_CHANGED/COMMENT/TASK_ADDED/TASK_COMPLETED), reusing the audit-log/event pattern; DealTask is a simple checklist. Additive migration applied to production; client regenerated; API builds clean | `556d709` | Pushed |
| 72 | 2026-07-26 | Deal Management — unit 2, the API. DealsModule with per-deal access control (parties + admin): GET /deals (founder/investor own, admin all, with counterparty names + stage/status), GET /deals/:id (full timeline + task checklist), PATCH /deals/:id/stage (any stage fwd/back; updates OPEN/WON/LOST; logs a STAGE_CHANGED timeline entry "Old → New"), POST comments (logs COMMENT), POST tasks + PATCH task toggle (logs TASK_ADDED / TASK_COMPLETED). The intro-accept flow now creates the Deal idempotently (resolving founder/investor by role) — the central object is never created from scratch. Wired into app.module; API builds clean | `d35e062` | Pushed |
| 73 | 2026-07-26 | Deal Management — unit 3, the frontend. /deals dashboard lists the caller's deals (counterparty from the viewer's perspective, stage + status badges; admins see all as founder↔investor). /deals/:id detail: a stage picker (any stage, forward or back — both parties see the change), a task checklist (add + toggle, strike-through on done), and an activity feed that is both the timeline (created/stage/task events) and the comment thread (post a comment). Deal state is re-read from each mutating call so it stays in sync. Added a Deals nav link for founder/investor/admin (desktop + mobile) and routes. Web builds clean | `f284e4e` | Pushed |
| 74 | 2026-07-26 | Deal Management — unit 4, browser verification on the deployed site + one UI fix. Full flow confirmed: the deal created from the earlier accepted intro (Verdant Labs ↔ Lumen Capital) shows on the founder's /deals dashboard; opened it and, through the UI, moved the stage (Meeting Scheduled → Data Room Access), posted a comment and added a task — timeline grew to 10 events; then logged in as the investor and opened the same deal to confirm identical state (stage Data Room Access, the founder's comment visible, same 10-event timeline, same checklist). Fixed a real bug the screenshot exposed: the native stage `<select>` used `[value]` binding, which Angular doesn't reliably reflect onto the selected `<option>`, so the control showed "Interest" while the deal was actually at Data Room Access — switched to `[selected]="s.value === d.stage"` per option so the picker shows the true current stage. Web builds clean | `24b40ff` | Pushed |
| 75 | 2026-07-26 | Auto Teaser + Information Memorandum generation (PRD v2 §3/§7 — the core differentiator) — unit 1, the backend. New DocumentsModule with a reusable core PdfService (§14.3): callers describe a document as an ordered block model (heading/paragraph/key-values/table/bullets/callout) and the service owns the look — brand charcoal-green palette, Helvetica, page-break-aware tables, a cover and page-numbered footers — using only pdfkit's built-in AFM fonts so nothing is read from disk beyond the library itself. FounderDocumentsService assembles the Teaser (1-page: pitch, snapshot, funding ask, key metrics if Financials shared) and the IM (full profile: overview, USP, business model, products, market, competition, SWOT, promoters, group, customers, suppliers, operations, journey, funding ask, then the gated commercial sections). Gating is NOT re-implemented — gated sections are fetched through the same FounderProfileService.financials/fundingHistory/shareholding/projectedFinancials/risks/futurePlans methods the product page uses, so a viewer who can't see a section on the page can't see it in the IM either, and each inclusion is audited identically; a "not shared" 404 just omits that section. Endpoints GET /founders/:userId/teaser.pdf and /im.pdf (JWT + FOUNDER/INVESTOR/ADMIN), streamed as application/pdf, Cache-Control no-store (regenerated every request, never stale). Smoke-tested locally: valid %PDF bytes render. API builds clean | `18f3caa` | Pushed |
| 76 | 2026-07-26 | Auto Teaser + IM — unit 2, the frontend actions. New DocumentsService fetches the PDF as a blob (so the auth interceptor attaches the JWT — a plain `<a download>` couldn't) and saves it via a transient object URL. A small reusable DocDownloadsComponent renders "Download Teaser" + "Generate IM" with a preparing/disabled state and an error line; it takes the founder userId + company name (used to name the file). Wired into the founder's own profile editor (own userId) and onto a founder's product page (for investors/admins), so the same action serves both audiences and the server decides per-viewer what the IM contains. Also surfaced companyName in the ownSections payload (it had been dropped) so the editor can name the download. Web + API build clean | `a7b7828` | Pushed |
| 77 | 2026-07-26 | Auto Teaser + IM — unit 3, browser + content + gating verification on the deployed site. Both endpoints return 200 application/pdf with valid %PDF bytes (teaser ~5.7KB, IM ~9.3KB). Decompressed the PDF content streams to prove the documents carry REAL structured data, not placeholders: the IM for the seeded founder (Verdant Labs / Verdant Grid) contains the actual company overview, USP ("Only platform scoring 2nd-life batteries"), business model, market, promoter (Asha Rao 55%), operations, company journey (First enterprise contract — Achieved), funding ask, financials with computed ratios (burn multiple 5.59x, ARR:MRR 12x), funding history (Lumen Capital, Northwind Angels), shareholding, projected financials, risks and future plans. Gating proven at the document level: as an investor, with Financials set PRIVATE the generated IM omits the Financials section (1277 chars), and set VISIBLE_TO_INVESTORS it includes it (1630) — the IM never leaks a gated section the viewer isn't entitled to, reusing the same audited gate as the profile page. UI: the Documents card + "Download Teaser"/"Generate IM" buttons render both on the founder's own editor and on a founder's product page for investors (investor pulled the IM at 8636 bytes — smaller, reflecting the sections not shared with them). No code change (product code shipped in 75/76; a stream-extraction bug was in the throwaway test script only) | `n/a (verification)` | Logged |
| 78 | 2026-07-26 | Admin CMS (original Day 3 scope, never built after the VMB pivot) — unit 1, the backend. New CmsModule over the existing key-value cms_content table, with a field registry (cms-fields.ts) as the single source of truth for every editable field — homepage headline/subtext, three pricing tiers (name/price/features), and the admin-configurable success-fee % (PRD §2, no longer hardcoded) — supplying each field's default, label, group and type so writes are validated against known keys and the admin form + upload template are generated from one list. CmsService reads grouped content for the editor, a world-readable publicContent() the homepage renders from, upserts edits transactionally (rejecting a nonsensical success fee), parses an uploaded .xlsx via exceljs (locates the key/value columns, keeps only known keys, preserves multiline feature lists), and streams a prefilled template workbook. Endpoints: GET/PUT /admin/cms, POST /admin/cms/bulk (FileInterceptor, 2MB cap), GET /admin/cms/template — all gated ADMIN + Ops/Finance sub-role (Super implicit); plus public GET /config/content. Smoke-tested locally: template→parse round-trips all 12 keys with newlines intact. API builds clean | `ade68bd` | Pushed |
| 79 | 2026-07-26 | Admin CMS — unit 2, the frontend + live homepage. New /admin/cms page (roleGuard ADMIN, "Site content" nav link for admins, desktop + mobile): renders whatever grouped fields the API returns (homepage copy / pricing tiers / settings) as edit inputs, Save persists all fields at once, and an Excel section downloads the prefilled template and uploads a filled one with a result summary (applied vs ignored-unknown keys). New CmsService (blob download keeps the JWT on the template request). The homepage now reads GET /config/content live — headline, subtext, the three pricing tiers and the success-fee line all come from cms_content with the previous hardcoded copy kept only as an instant/failure fallback; removed the stale "frontend scaffold" note. Documented the Excel template + editable keys in Docs/CMS_Bulk_Upload_Template.md. Web + API build clean | `2f8cc4c` | Pushed |
| 80 | 2026-07-26 | Admin CMS — unit 3, browser verification on the deployed site (all four requirements). (1) GET /admin/cms returns 12 fields across homepage(2)/pricing(9)/settings(1). (2) A single field edit via the admin UI ("Saved 12 fields. Live on the homepage now.") — the edited headline ("Sync up. Raise smarter. — UI edit") then rendered on the live homepage, proving it reads cms_content live, not hardcoded. (3) Excel bulk upload: generated an .xlsx with the key/value columns changing four fields at once (headline, subtext, tier1 name, success fee→6) plus one bogus key; POST /admin/cms/bulk returned updated:4 with the bogus key in skippedKeys, and all four changes showed on the live homepage in one shot. (4) The success fee is an editable Settings field (default 5, changed to 6 and back), no longer hardcoded (PRD §2). The demo admin is OPS, which the Ops/Finance gate allows. Reset the homepage copy back to defaults after the test. No code change | `n/a (verification)` | Logged |
| 81 | 2026-07-26 | Real public landing page — unit 1, public stats endpoint. Added GET /stats (public, unauthenticated) returning live platform counters for the marketing homepage — founders (founderProfile count), investors (investorProfile count), deals, and dealsClosed (WON) — computed on request since the platform_stats snapshot table still has no writer wired. Only aggregate non-identifying totals, safe to serve unauthenticated. New StatsModule wired into app.module; API builds clean | `0b78ffc` | Pushed |
| 82 | 2026-07-26 | Real public landing page — unit 2, the frontend. Replaced the scaffold homepage with a full marketing landing page rendering its own charcoal nav + footer (the app-shell header/footer + max-width container are now suppressed on the '/' route, tracked via NavigationEnd, so the page is full-bleed). Sections: sticky nav (logo, Home / For Investors / For Founders / How it Works / Discover / Pricing / About, Log in + Get Started — auth-aware: signed-in visitors get "Go to app" to their real discover/dashboard route; mobile hamburger menu); hero (CMS headline + subtext + two role CTAs to /signup); live stats row (real counts from GET /stats — founders/investors/active deals/deals closed); For Founders (structured profile, one-click Teaser/IM, private data room, discovery) and For Investors (targeted discovery, financials on request, data room access) feature grids; How it Works (profile → discover → deal, reflecting the real Deal Management flow); Pricing (the three CMS tiers + success-fee line); About; and a Product/Company/Legal footer. Charcoal-green tokens + PrimeNG p-buttons throughout, responsive grids with a mobile nav. Nav section links are same-page anchors; Discover/Log in/Get Started go to real routes. Web builds clean | `95beb27` | Pushed |
| 83 | 2026-07-26 | Real public landing page — unit 3, browser verification on the deployed site (desktop + mobile). Content is genuinely CMS/stats-driven, not hardcoded: the rendered headline and subtext exactly match GET /config/content, the pricing tier names match the CMS tiers, and the stats row (3 founders / 2 investors / 1 active deal / 0 closed) exactly matches the live GET /stats. All five section anchors (for-founders/for-investors/how-it-works/pricing/about) present; app-shell header correctly suppressed on '/'; no horizontal overflow at 1280px or 375px. Desktop: full nav (Home/For Investors/For Founders/How it Works/Pricing/About/Discover) renders; logged out shows Log in + Get Started, signed in shows "Go to app" to the real discover/dashboard route. Mobile 375px: nav collapses to a hamburger, the menu lists all links, hero + sections stack single-column, footer reflows. Screens captured for hero/stats, feature grids, how-it-works, about + footer, and the mobile menu. No code change | `n/a (verification)` | Logged |
| 84 | 2026-07-26 | Approved homepage additions — unit 1, simple CMS fields (same registry pattern). Added homepage.tagline (a short pre-headline line above the hero H1) and a closing-CTA banner (homepage.cta.headline / .subtext / .button) to cms-fields.ts, and surfaced them in publicContent() under homepage.tagline and homepage.cta. Editable from the existing admin Site-content page and included in the bulk-upload template automatically (registry-driven). API builds clean | `32933ce` | Pushed |
| 85 | 2026-07-27 | Approved homepage additions — unit 2, the repeatable-content data model (one shared table, not six). Added a single CollectionItem model backing all six collections (sample listings, match preview, team, testimonials, blog, achievements) via a CollectionKind enum, with the union of fields each needs (title/subtitle/body/linkUrl/matchPct/sector/date/imageId/sortOrder/published) so they share one CRUD/admin shape; plus a CmsImage table (fileName/contentType/sizeBytes) for admin-uploaded marketing images (team photos, blog covers, avatars) whose bytes live in the shared FileStorage. Migration generated via `migrate diff` against the live DB (purely additive — CREATE TYPE + two tables + index + FK), applied to prod Neon (dry-bonus-70315470) with `migrate deploy`, client regenerated, tables verified (both empty). API builds clean | `d69a902` | Pushed |
| 86 | 2026-07-27 | Approved homepage additions — unit 3, the shared collections API. One CollectionsService/controller pair serves all six collections generically, driven by a declarative collection-specs.ts (slug↔kind, per-collection field list, illustrative/image/hide-when-empty flags) that is the single source of truth for both validation and the admin form. Admin (ADMIN + Ops): GET /admin/collections/specs, GET/POST /admin/collections/:slug, PUT/DELETE /admin/collections/item/:id, POST /admin/collections/image (multipart, 4MB cap, image/* only → CmsImage + FileStorage.put). The service keeps only the columns each collection's spec declares, so a stray field can't land on the wrong kind, and sanitises link URLs (http(s)/relative only, never javascript:/data:). Public (unauthenticated): GET /config/collections (published items grouped for the homepage; match preview = first entry; blog teaser = latest 3 with excerpts), GET /config/blog + /config/blog/:id, and GET /cms/media/:id (redirects to a presigned S3 URL or streams from Postgres). Wired into app.module; API builds clean | `c699594` | Pushed |
| 87 | 2026-07-27 | Approved homepage additions — unit 4, the generic admin collections editor. One /admin/collections page (roleGuard ADMIN, new "Homepage content" admin nav link) manages all six collections: a pill switcher across the server's specs, a list of existing entries (thumbnail + title + hidden badge + Edit/Remove), and an add/edit form generated from the selected collection's field spec — text/textarea/number/date inputs, an image upload (for team/testimonials/blog) with preview, plus Published and Sort-order controls. Add/edit/remove all re-read from the server. New CollectionsService (blob-free JSON + multipart image upload; mediaUrl() turns the API-relative media path absolute). Because the form is spec-driven, the same page covers sample listings, match preview, team, testimonials, blog and achievements with no per-collection code. Web builds clean | `8b8503a` | Pushed |
| 88 | 2026-07-27 | Approved homepage additions — unit 5, the homepage sections. Rendered the hero pre-headline tagline and the closing CTA banner (from the CMS fields) after the pricing section and before the footer. Added the repeatable sections from GET /config/collections: sample listing cards and the founder↔investor match preview — both carrying a prominent amber "Illustrative example" badge plus an italic "not real founders/live data" note (dashed borders reinforce they're mockups); testimonials, team (round photos or a placeholder), and a blog teaser (latest three, links to /blog and posts) — each wrapped in @if so the section is fully hidden when its collection is empty. Footer gained a conditional "Success stories" column (achievement links, hidden when none) and its Privacy/Terms now link to the real legal routes instead of muted text. Charcoal-green tokens, responsive grids. Web builds clean | `2203c48` | Pushed |
| 89 | 2026-07-27 | Approved homepage additions — unit 6, blog pages + real legal pages. Public /blog list (cards from GET /config/blog, empty-state message) and /blog/:id post page (cover, date, whitespace-preserved body). Real Privacy Policy (/legal/privacy) and Terms of Service (/legal/terms) rendered by one LegalComponent selecting the document from route data — genuine SaaS/marketplace boilerplate covering data collection/use/sharing, retention & security, user rights, and (in Terms) the explicit not-a-broker/adviser and no-guarantee-of-investment disclaimers, user due-diligence + confidentiality responsibilities, liability limits and indemnity. Each legal page carries a prominent banner stating it is placeholder boilerplate not yet reviewed by a lawyer. Footer Privacy/Terms now resolve to these pages. NOTE flagged to the user: the legal copy is placeholder and must be reviewed by counsel before real users rely on it. Web builds clean | `7fcb3e3` | Pushed |
| 90 | 2026-07-27 | Approved homepage additions — fix found during testing: the collections controller declared `@Post('image')` after `@Post(':slug')`, so a multipart image upload to /admin/collections/image was routed to create() with slug="image" and failed DTO validation ("title must be a string"). Reordered the static segments (image, item/:id) ahead of the ':slug' wildcards so uploads and item updates route correctly. API builds clean | `38327bf` | Pushed |
| 91 | 2026-07-27 | Approved homepage additions — final browser verification on the deployed site (all requirements). CMS fields: hero tagline + closing CTA render from cms_content and a live edit of homepage.tagline via the CMS save endpoint showed on GET /config/content immediately (reset after). Hide-when-empty: with zero entries, all six repeatable sections were absent from the homepage and the footer Success-stories column too; after seeding via the admin API (which exercised the image-upload endpoint end-to-end — team/testimonial/blog images stored and served through GET /cms/media/:id, images loaded with naturalWidth>0), every section appeared. Illustrative labelling: the sample-listing cards and match-preview card each show a prominent amber "ILLUSTRATIVE EXAMPLE" badge plus the "not real founders/live data" note (confirmed visually at desktop + 375px). Admin CRUD via the UI: added an achievement (1→2, "Entry added"), edited its title, and removed it (2→1) — all reflected in the list. Legal: /legal/privacy (7 sections) and /legal/terms (9 sections, incl. the not-a-broker and no-guarantee-of-investment disclaimers) render with the placeholder-content banner; footer Privacy/Terms resolve to /legal/privacy and /legal/terms. Blog: /blog lists the post and /blog/:id renders the full body. No horizontal overflow at 1280/375. Cleanup: removed the fabricated team/testimonial/achievement test entries afterwards (those must be real), leaving the badged illustrative sample listings + match preview and the genuine blog post as demo content. No code change | `n/a (verification)` | Logged |
| 92 | 2026-08-02 | Dependency hygiene + NestJS upgrade. (1) Reported the @angular/* version state and found the inconsistency lived entirely in uncommitted working-tree edits (an external `npm install`/`ng update` had bumped the web builder to v22, added a bogus @angular-devkit/build-angular@22 to the API, half-upgraded NestJS 10→11, downgraded exceljs, +30k lockfile lines); committed HEAD was already a clean, consistent Angular 21 that Vercel deploys. Per the user's choice, discarded all of it (git checkout of the 5 files + `npm ci`) — restored Angular 21 / build-angular 21 / NestJS 10 / exceljs 4; both apps build clean, tree clean. (2) Then, per the user, upgraded ONLY the API to NestJS latest while holding Angular at 21 (Angular 22 needs TS 6.0 and PrimeNG 21 is Angular-21-only — keeping PrimeNG watermark-free ruled out the Angular jump). Bumped @nestjs/{common,core,platform-express}→^11.1.28, @nestjs/config→^4.0.4, @nestjs/jwt→^11.0.2, @nestjs/{cli,schematics}→^11, and express→^5.1.0 + @types/express→^5 (NestJS 11 brings Express 5). No wildcard routes and only Express-5-safe raw-express usage (serverless bootstrap + res.setHeader/end/redirect), so no code changes needed. Verified: API builds clean; booted the compiled app in-process against the prod DB — /api/health (db up), /api/stats, /api/config/collections, /api/config/content all 200; POST /api/auth/demo-login issues a JWT (jwt@11 + Express-5 body parsing) and an invalid body 400s (ValidationPipe). Angular + PrimeNG 21 untouched | `d276664` | Pushed |
| 93 | 2026-08-02 | Lint cleanup — made `npm run build` and `npm run lint` clean across the monorepo. (1) Root: added a `build` script (`npm run build --workspaces --if-present`) so `npm run build` works at the repo root (previously only api:build/web:build existed → "Missing script: build"). (2) Web ESLint: committed the previously-untracked apps/web/eslint.config.js and the `lint` architect target ng added to angular.json (stripped the auto-added analytics telemetry id). Cleared all 52 `ng lint` errors: `array-type` (Array<T>→T[]) via ng lint --fix; template `eqeqeq` fixed correctly by allowing the idiomatic `x != null` nullish check (rule option `allowNullOrUndefined:true`) rather than mechanically switching to `!== null`, which would have broken `Record` ratio lookups where a missing key is `undefined`; `elements-content` on `<a pButton label="…">` fixed by moving the label into child text (PrimeNG renders projected content identically); `label-has-associated-control` fixed by associating labels with their controls (for/id) or converting genuine group captions to a styled `.field-label` span. (3) API ESLint: it had a lint script but no config or deps (ESLint 9+ needs a flat config) → added apps/api/eslint.config.mjs (typescript-eslint recommended, ignoreRestSiblings for destructure-to-omit, `_`-prefix ignores) + eslint/typescript-eslint/@eslint/js/globals devDeps, made the script report-only (`eslint src`), and removed one genuinely-unused import. Both apps build clean; `npm run lint` → all pass. No runtime/behaviour change | `8dc83bf` | Pushed |
| 94 | 2026-08-02 | Added the two missing bits of dev tooling the user hit (`npm run test` had no script/tests; `docker compose` had no file). (1) Tests: set up Jest + ts-jest in the API (jest 30 / ts-jest 29.4, config, `test`/`test:watch` scripts + a root `test` script, jest globals in the ESLint flat config for *.spec.ts). Wrote 20 real unit tests across 4 suites — pitch-video URL parsing (the security-sensitive one: accepts youtube/vimeo/loom shapes, rejects javascript:/data:/file: schemes, lookalike hosts like youtube.com.evil.com, and ids outside the safe charset), the cms-fields registry (known-key validation incl. __proto__, toList splitting), the collection specs (unique slugs/kinds, illustrative + hide-when-empty + image flags), and the PdfService (renders valid %PDF bytes across every block type). All 20 pass. (2) Local Postgres: added docker-compose.yml (postgres:16-alpine, healthcheck, named volume) + root db:up/db:down/db:logs scripts, a .env.example local-DB block, and Docs/Local_Development.md (start db → set .env → prisma:deploy → api:dev/web:dev; incl. the PowerShell execution-policy note). `docker compose config` validates; the live container spin-up needs Docker Desktop running (daemon was off here) but Prisma migrations are already proven against real Postgres. npm run test/lint/build all green. Left the auto-re-added angular.json analytics id uncommitted (not committing telemetry) | `_pending_` | Pushed |
